const { writeFile } = require('./fileModule.js');
const { read } = require('./fileModule.js');


  function addMultiply(word, changeWord) {
    let result = '';
    for (let i = 0; i <10; i++) {
        result += word;
     
    }
    result += extraWord
    result += word;
    for (let i = 0; i < result.length; i++) {
        console.log(`Index: ${word}, Value: ${result[i]}`);
      }
      word.index === 501 ? result "How is it going " , result "i am learning backend at blockfuse labs" 


    return result;



}

writeFile('classWork.txt', addMultiply ("i am learning backend at blockfuse labs \n"));
// read("classwork.txt");


